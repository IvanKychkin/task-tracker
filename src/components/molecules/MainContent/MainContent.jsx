import React from "react";
import styled from "styled-components";

const StyledMainContent = styled.main(
    ({ color }) => css`
      display: flex;
      flex-direction: column;
    `,
  );

  const MainContent = () => {
    return <MainContent>Main content here</MainContent>;
  };
  //кнопочку сюда
  
export default MainContent;